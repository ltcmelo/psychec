// struct names created by the solver follow the convencion Tx, where x
// is an integer (although the exact ordering is undefined)

void f() {
    a.x = 6.6;
    b->y = 1.0;
    c->z = "foo";
    c->w = 10;
    d->next = d;
    d->val = 9.0;
    return;
}
